// Autogenerated using npm run gen MatrixActionTable
/*
 * Copy to other files
 * import MatrixActionTable from './MatrixActionTable';
 * <MatrixActionTable/>
 */
import styled from 'styled-components';
// import PropTypes from 'prop-types';
// import { Link, Switch, Route } from 'react-router-dom';

function MatrixActionTable(props) {
  const matrixAction = props.actions.map((item, key) => (
    <tr key={key}>
      <td>{item.name}</td>
      <td>{item.actionType}</td>
      <td>{item.limit.offense}</td>
      <td>{item.test.offense.join(' + ')}</td>
      <td>placeholder</td>
      <td>placeholder</td>
      <td>placeholder</td>
      <td>placeholder</td>
    </tr>
  ));
  return (
    <Main className="container-fluid d-flex justify-content-center">
      <table className="table bg-light">
        <thead>
          <tr>
            <th scope="col">Matrix Action</th>
            <th scope="col">Type</th>
            <th scope="col">Limit</th>
            <th scope="col">Roll</th>
            <th scope="col">Value</th>
            <th scope="col">Opposed Roll</th>
            <th scope="col">Value</th>
            <th scope="col">Marks Req.</th>
            <th scope="col">Effect</th>
          </tr>
        </thead>
        <tbody>{matrixAction}</tbody>
      </table>
    </Main>
  );
}

MatrixActionTable.propTypes = {
  values: PropTypes.object,
  actions: PropTypes.array,
};

export default MatrixActionTable;

const Main = styled.div`
  font-family: sans-serif;
  padding-top: 10px;
  font-size: 1em;
`;
